files:
  "/etc/cron.d/my_cron_job":
    mode: "000644"
    owner: root
    group: root
    content: |
      # Runs the Django management command at the specified time
      08 15 * * * root /path/to/load_and_run.sh >> /var/log/my_cron_job.log 2>&1
