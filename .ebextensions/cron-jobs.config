files:
  "/etc/cron.d/reset_likes_cron":
    mode: "000644"
    owner: root
    group: root
    content: |
      # Runs the Django management command at 10:10 AM UTC every day
      0 11 * * * root . /var/app/venv/staging-LQM1lest/bin/activate && /var/app/venv/staging-LQM1lest/bin/python /var/app/current/manage.py reset_likes >> /var/log/reset_likes.log 2>&1
commands:
  remove_old_cron_job:
    command: "rm -f /etc/cron.d/reset_likes_cron.bak"

